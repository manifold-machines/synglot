from setuptools import setup, find_packages

setup(
    name='synglot',
    version='0.1.0',
    packages=find_packages(),
    # Add other parameters as needed, like:
    # install_requires=[
    # 'numpy',
    # 'pandas',
    # ],
    # entry_points={
    # 'console_scripts': [
    # 'synglot_cli=synglot.cli:main',
    # ],
    # },
) 